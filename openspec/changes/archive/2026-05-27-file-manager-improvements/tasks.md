## 1. 文件列表列头排序

- [x] 1.1 给名称、大小、修改时间三列添加 `sortable="custom"` 属性
- [x] 1.2 在 `el-table` 添加 `@sort-change` 事件处理
- [x] 1.3 实现自定义排序逻辑：目录始终排在文件前面，同类型内按排序列排序
- [x] 1.4 排序状态显示支持升序/降序切换

## 2. 面包屑增强

- [x] 2.1 面包屑右侧添加"编辑路径"按钮和"复制路径"按钮
- [x] 2.2 实现面包屑/路径输入框切换模式：点击编辑按钮显示 `el-input` 预填当前路径
- [x] 2.3 实现回车跳转：输入路径后按 Enter 验证并导航，无效路径提示错误
- [x] 2.4 实现复制路径：点击复制按钮调用 `navigator.clipboard.writeText` 并提示成功

## 3. 编辑器移动端适配

- [x] 3.1 在样式区追加 `@media (max-width: 768px)` 规则，设置 `.cm-editor` 字号为 12px
- [x] 3.2 移动端编辑器弹窗标题字号调小
- [x] 1.5 可编辑文件双击直接打开编辑器
